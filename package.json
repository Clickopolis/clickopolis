{
  "scripts": {
    "test": "lerna run --parallel test",
    "publish": "lerna publish --conventional-commits -m 'chore(release): publish :tada:' --independent",
    "build": "lerna run --parallel  build",
    "docs": "node ./scripts/write-docs.js",
    "start:app": "cd packages/app && npm start"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  },
  "dependencies": {
    "tslib": "^1.8.1",
    "typescript": "^2.9.2"
  }
}
