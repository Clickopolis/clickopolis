{
  "scripts": {
    "test": "lerna run --parallel test",
    "publish": "lerna publish --conventional-commits -m 'chore(release): publish :tada:'",
    "build": "lerna run --parallel  build",
    "docs": "node ./scripts/write-docs.js",
    "preinstall": "bash preinstall.sh",
    "start:app": "cd packages/app && npm start",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  },
  "dependencies": {
    "tslib": "^1.8.1",
    "typescript": "^3.3.4000"
  },
  "name": "clickopolis"
}
